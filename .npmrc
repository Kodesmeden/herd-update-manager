# The registry's advisory endpoint is unreliable and npm waits up to
# fetch-timeout (300s) with two retries before giving up, which turns a
# two-second install into a ten-minute one. This only skips the audit that
# npm runs implicitly during install and update. Running `npm audit`
# yourself still works exactly as before.
audit=false
